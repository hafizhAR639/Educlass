<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="90dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="12dp"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:clickable="true"
    android:focusable="true">

    <!-- The colored background pill -->
    <View
        android:id="@+id/layout_subject_bg"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/shared_bg_rect_rounded"
        android:backgroundTint="#D1E9FF" />

    <!-- The circular icon container -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_icon_wrapper"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_centerHorizontal="true"
        app:cardCornerRadius="32dp"
        app:cardElevation="4dp"
        app:strokeWidth="1.5dp"
        app:strokeColor="#000000">

        <ImageView
            android:id="@+id/iv_subject_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/shared_ic_math" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Subject Name label at the bottom of the pill -->
    <TextView
        android:id="@+id/tv_subject_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/layout_subject_bg"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="12dp"
        android:text="Mathematics"
        android:textColor="#000000"
        android:textSize="10sp"
        android:textStyle="bold"
        android:gravity="center"
        android:ellipsize="end"
        android:maxLines="1" />

</RelativeLayout>
